<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder" xmlns:g="urn:import:com.google.gwt.user.client.ui"
  xmlns:w="urn:import:com.google.gwt.widget.client" xmlns:c="urn:import:com.google.gwt.user.cellview.client"
  xmlns:app="urn:import:com.retech.reader.web.client.mobile.ui">

  <ui:style>
    .outer {
      background: #eee;
    }
    
    .title {
      background: #393939;
      color: white;
      padding: 4px 10px;
      font-size: 10pt;
    }
    
    .editForm {
      padding: 10px;
      background: white;
    }
    
    .label {
      color: #666;
      font-size: 10pt;
      padding-bottom: 3px;
    }
    
    .field {
      width: 100%;
      margin-bottom: 12px;
    }
    
    .textBoxWrapper {
      margin-right: 10px;
    }
    
    .nameBox {
      height: 2em;
    }
    
    .notesBox {
      height: 4em;
    }
    
    .violation {
      color: red;
    }
    
    .button {
      width: 100%;
    }
    
    input[type="text"] {
  		width: 100%;
    }
    
    .hbox {
      display: -webkit-box;
      -webkit-box-orient: horizontal;
    }
    
    .hbox>* {
      -webkit-box-flex: 1;
      padding: 10px;
    }
  </ui:style>

  <g:DockLayoutPanel unit="PT">
    <!-- Title. -->
    <g:north size="18">
      <g:Label addStyleNames="{style.title}">编辑</g:Label>
    </g:north>

    <g:center>
      <g:ScrollPanel addStyleNames="{style.outer}">
        <g:HTMLPanel>

          <!-- Edit Form. -->
          <div class="{style.editForm}">
            <!-- Task name. -->
            <div class="{style.label}">标题</div>
            <div class="{style.textBoxWrapper}">
              <c:CellWidget addStyleNames="{style.field} {style.nameBox}" ui:field="nameField" />
            </div>
            <div ui:field="nameViolation" class="{style.violation}">
            </div>

            <!-- Task notes. -->
            <div class="{style.label}">简介</div>
            <div class="{style.textBoxWrapper}">
              <c:CellWidget addStyleNames="{style.field} {style.notesBox}" ui:field="detailEditor" />
            </div>

            <!-- Task due date. -->
            <div class="{style.label}">创建日期</div>
            <c:CellWidget addStyleNames="{style.field}" ui:field="createTimeEditor" />
          </div>

          <!-- Button panel. -->
          <div class="{style.hbox}">
            <div>
              <w:TextButton ui:field="playButton" addStyleNames="{style.button}">阅读</w:TextButton>
            </div>
            <div>
              <w:TextButton ui:field="deleteButton" decoration="NEGATIVE"
                addStyleNames="{style.button}">删除</w:TextButton>
            </div>
          </div>

        </g:HTMLPanel>
      </g:ScrollPanel>
    </g:center>
  </g:DockLayoutPanel>

</ui:UiBinder> 
